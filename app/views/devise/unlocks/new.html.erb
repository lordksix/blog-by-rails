<div class="flex flex-col w-full max-w-3xl mx-auto gap-6 justify-center align-center">
  <%= form_for(resource, as: resource_name, class: 'flex flex-col w-full px-4 py-2 border-2 border-black max-w-3xl mx-auto gap-6 justify-center align-center', url: unlock_path(resource_name), html: { method: :post }) do |f| %>
    <div class= 'flex flex-col px-4 py-2 border-2 border-black w-full max-w3xl mx-auto gap-6 justify-center align-center'>
      <h1 class="text-center">Resend unlock instructions</h1>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="field">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
      </div>

      <div class="actions px-4 py-2 border-2 border-black self-start">
        <%= f.submit "Resend unlock instructions" %>
      </div>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
